dist: xenial
language: cpp
compiler: clang

before_install:
  - sudo apt-get install libjemalloc-dev libpcre3-dev
  - find /usr/local -name string_view

install: autoreconf -if

script: CXXFLAGS='-stdlib=libc+' ./configure && make -j4 && make test
